{
  "name": "bi-now-playing-app-express",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && yarn start",
    "server": "nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "dependencies": {
    "express": "^4.16.2",
    "react-bootstrap": "^0.32.3",
    "socket.io": "^2.1.1",
    "twitter": "^1.7.1"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
